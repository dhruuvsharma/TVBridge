<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Background -->
    <SolidColorBrush x:Key="AppBackground" Color="#F0F0F8" />
    <SolidColorBrush x:Key="SidebarBackground" Color="#1A1A2E" />
    <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFF" />
    <SolidColorBrush x:Key="Surface2Brush" Color="#E8E8F4" />
    <SolidColorBrush x:Key="StatusBarBackground" Color="#E4E4F0" />

    <!-- Accent / Neon -->
    <SolidColorBrush x:Key="NeonCyan" Color="#0099BB" />
    <SolidColorBrush x:Key="NeonMagenta" Color="#BB00BB" />
    <SolidColorBrush x:Key="NeonGreen" Color="#00AA55" />
    <SolidColorBrush x:Key="NeonAmber" Color="#CC8800" />
    <SolidColorBrush x:Key="NeonRed" Color="#CC2244" />
    <SolidColorBrush x:Key="AccentBrush" Color="#0099BB" />

    <!-- Text -->
    <SolidColorBrush x:Key="TextPrimary" Color="#1A1A2E" />
    <SolidColorBrush x:Key="TextSecondary" Color="#6666AA" />
    <SolidColorBrush x:Key="TextMuted" Color="#9999BB" />

    <!-- Borders -->
    <SolidColorBrush x:Key="BorderBrush" Color="#CCCCDD" />
    <SolidColorBrush x:Key="BorderHover" Color="#0099BB" />

    <!-- Nav (sidebar stays dark for neon effect in both themes) -->
    <SolidColorBrush x:Key="NavHoverBackground" Color="#2A2A44" />
    <SolidColorBrush x:Key="NavActiveBackground" Color="#222240" />
    <SolidColorBrush x:Key="NavActiveBorder" Color="#00CCEE" />
    <SolidColorBrush x:Key="NavText" Color="#8888AA" />
    <SolidColorBrush x:Key="NavActiveText" Color="#00CCEE" />

    <!-- Buttons -->
    <SolidColorBrush x:Key="ButtonBackground" Color="#E8E8F4" />
    <SolidColorBrush x:Key="ButtonHoverBackground" Color="#D8D8EC" />
    <SolidColorBrush x:Key="ButtonForeground" Color="#0099BB" />
    <SolidColorBrush x:Key="ButtonPrimaryBackground" Color="#0099BB" />
    <SolidColorBrush x:Key="ButtonPrimaryForeground" Color="#FFFFFF" />

    <!-- Input -->
    <SolidColorBrush x:Key="InputBackground" Color="#FFFFFF" />
    <SolidColorBrush x:Key="InputBorder" Color="#CCCCDD" />
    <SolidColorBrush x:Key="InputFocusBorder" Color="#0099BB" />
    <SolidColorBrush x:Key="InputForeground" Color="#1A1A2E" />

    <!-- DataGrid -->
    <SolidColorBrush x:Key="GridRowBackground" Color="#FFFFFF" />
    <SolidColorBrush x:Key="GridAltRowBackground" Color="#F5F5FC" />
    <SolidColorBrush x:Key="GridHeaderBackground" Color="#E8E8F4" />
    <SolidColorBrush x:Key="GridHeaderForeground" Color="#6666AA" />
    <SolidColorBrush x:Key="GridSelectedBackground" Color="#D0E8F0" />
    <SolidColorBrush x:Key="GridLineBrush" Color="#E0E0EC" />

    <!-- Cards -->
    <SolidColorBrush x:Key="CardBackground" Color="#FFFFFF" />
    <SolidColorBrush x:Key="CardBorder" Color="#DDDDEE" />

    <!-- Status -->
    <SolidColorBrush x:Key="SuccessBackground" Color="#E8F8F0" />
    <SolidColorBrush x:Key="SuccessForeground" Color="#00AA55" />
    <SolidColorBrush x:Key="WarningBackground" Color="#FFF8E8" />
    <SolidColorBrush x:Key="WarningForeground" Color="#CC8800" />
    <SolidColorBrush x:Key="ErrorBackground" Color="#FFF0F4" />
    <SolidColorBrush x:Key="ErrorForeground" Color="#CC2244" />

    <!-- Glow Effects (subtler in light mode) -->
    <DropShadowEffect x:Key="NeonGlow" Color="#0099BB" BlurRadius="8" ShadowDepth="0" Opacity="0.3" />
    <DropShadowEffect x:Key="NeonGlowMagenta" Color="#BB00BB" BlurRadius="8" ShadowDepth="0" Opacity="0.3" />
    <DropShadowEffect x:Key="NeonGlowGreen" Color="#00AA55" BlurRadius="8" ShadowDepth="0" Opacity="0.3" />
    <DropShadowEffect x:Key="CardShadow" Color="#000000" BlurRadius="12" ShadowDepth="1" Opacity="0.08" />

    <!-- Corner Radius -->
    <CornerRadius x:Key="CardRadius">8</CornerRadius>
    <CornerRadius x:Key="ButtonRadius">6</CornerRadius>
    <CornerRadius x:Key="InputRadius">4</CornerRadius>

    <!-- ===== CONTROL STYLES ===== -->

    <!-- NavButton -->
    <Style x:Key="NavButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource NavText}" />
        <Setter Property="BorderThickness" Value="3,0,0,0" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Padding" Value="16,12" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource NavHoverBackground}" />
                            <Setter Property="Foreground" Value="#E0E0FF" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- NeonButton (default) -->
    <Style x:Key="NeonButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource ButtonBackground}" />
        <Setter Property="Foreground" Value="{StaticResource ButtonForeground}" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="14,8" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource ButtonRadius}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource ButtonHoverBackground}" />
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.4" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- NeonButtonPrimary -->
    <Style x:Key="NeonButtonPrimary" TargetType="Button" BasedOn="{StaticResource NeonButton}">
        <Setter Property="Background" Value="{StaticResource ButtonPrimaryBackground}" />
        <Setter Property="Foreground" Value="{StaticResource ButtonPrimaryForeground}" />
        <Setter Property="BorderBrush" Value="{StaticResource ButtonPrimaryBackground}" />
    </Style>

    <!-- NeonTextBox -->
    <Style x:Key="NeonTextBox" TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource InputBackground}" />
        <Setter Property="Foreground" Value="{StaticResource InputForeground}" />
        <Setter Property="BorderBrush" Value="{StaticResource InputBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="8,6" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="CaretBrush" Value="{StaticResource AccentBrush}" />
        <Setter Property="SelectionBrush" Value="{StaticResource AccentBrush}" />
        <Style.Triggers>
            <Trigger Property="IsFocused" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource InputFocusBorder}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- NeonCheckBox -->
    <Style x:Key="NeonCheckBox" TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
        <Setter Property="FontSize" Value="13" />
    </Style>

    <!-- NeonComboBox -->
    <Style x:Key="NeonComboBox" TargetType="ComboBox">
        <Setter Property="Background" Value="{StaticResource InputBackground}" />
        <Setter Property="Foreground" Value="{StaticResource InputForeground}" />
        <Setter Property="BorderBrush" Value="{StaticResource InputBorder}" />
        <Setter Property="Padding" Value="8,6" />
        <Setter Property="FontSize" Value="13" />
    </Style>

    <!-- NeonCard -->
    <Style x:Key="NeonCard" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource CardBackground}" />
        <Setter Property="BorderBrush" Value="{StaticResource CardBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="{StaticResource CardRadius}" />
        <Setter Property="Padding" Value="16" />
        <Setter Property="Effect" Value="{StaticResource CardShadow}" />
    </Style>

    <!-- DataGrid Column Header -->
    <Style x:Key="NeonColumnHeader" TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{StaticResource GridHeaderBackground}" />
        <Setter Property="Foreground" Value="{StaticResource GridHeaderForeground}" />
        <Setter Property="BorderBrush" Value="{StaticResource GridLineBrush}" />
        <Setter Property="BorderThickness" Value="0,0,1,1" />
        <Setter Property="Padding" Value="8,6" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="FontWeight" Value="Bold" />
    </Style>

    <!-- DataGrid Row -->
    <Style x:Key="NeonDataGridRow" TargetType="DataGridRow">
        <Setter Property="Background" Value="{StaticResource GridRowBackground}" />
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
        <Setter Property="BorderThickness" Value="0" />
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource GridSelectedBackground}" />
                <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource Surface2Brush}" />
            </Trigger>
            <Trigger Property="AlternationIndex" Value="1">
                <Setter Property="Background" Value="{StaticResource GridAltRowBackground}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- DataGrid Cell -->
    <Style x:Key="NeonDataGridCell" TargetType="DataGridCell">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="6,4" />
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- NeonDataGrid -->
    <Style x:Key="NeonDataGrid" TargetType="DataGrid">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="RowBackground" Value="{StaticResource GridRowBackground}" />
        <Setter Property="AlternatingRowBackground" Value="{StaticResource GridAltRowBackground}" />
        <Setter Property="GridLinesVisibility" Value="Horizontal" />
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource GridLineBrush}" />
        <Setter Property="HeadersVisibility" Value="Column" />
        <Setter Property="AutoGenerateColumns" Value="False" />
        <Setter Property="IsReadOnly" Value="True" />
        <Setter Property="SelectionMode" Value="Single" />
        <Setter Property="CanUserSortColumns" Value="True" />
        <Setter Property="CanUserResizeRows" Value="False" />
        <Setter Property="ColumnHeaderStyle" Value="{StaticResource NeonColumnHeader}" />
        <Setter Property="RowStyle" Value="{StaticResource NeonDataGridRow}" />
        <Setter Property="CellStyle" Value="{StaticResource NeonDataGridCell}" />
    </Style>

    <!-- Page title -->
    <Style x:Key="PageTitle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
        <Setter Property="FontSize" Value="22" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style x:Key="PageSubtitle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextSecondary}" />
        <Setter Property="FontSize" Value="13" />
    </Style>

    <Style x:Key="SectionTitle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource NeonCyan}" />
        <Setter Property="FontSize" Value="15" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style x:Key="FieldLabel" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextSecondary}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Margin" Value="0,0,0,4" />
    </Style>

</ResourceDictionary>
