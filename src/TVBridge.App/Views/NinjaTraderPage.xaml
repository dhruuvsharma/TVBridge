<UserControl x:Class="TVBridge.App.Views.NinjaTraderPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Loaded="OnLoaded">
    <Grid Margin="24" Background="{DynamicResource AppBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="NinjaTrader" Style="{DynamicResource PageTitle}" Margin="0,0,0,4" />
            <TextBlock Text="Order routing to NinjaTrader 8 via ATI socket" Style="{DynamicResource PageSubtitle}" />
        </StackPanel>

        <!-- Config + Test -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- ATI Config -->
            <Border Grid.Column="0" Style="{DynamicResource NeonCard}" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="ATI Connection" Style="{DynamicResource SectionTitle}" Margin="0,0,0,12" />

                    <TextBlock Text="Host" Style="{DynamicResource FieldLabel}" />
                    <TextBox Text="{Binding Host, UpdateSourceTrigger=PropertyChanged}"
                             Style="{DynamicResource NeonTextBox}" Margin="0,0,0,10" />

                    <TextBlock Text="Port" Style="{DynamicResource FieldLabel}" />
                    <TextBox Text="{Binding Port, UpdateSourceTrigger=PropertyChanged}"
                             Style="{DynamicResource NeonTextBox}" Width="120" HorizontalAlignment="Left"
                             Margin="0,0,0,12" />

                    <CheckBox Content="Channel Enabled" IsChecked="{Binding IsEnabled}"
                              Style="{DynamicResource NeonCheckBox}" Margin="0,0,0,12" />

                    <StackPanel Orientation="Horizontal">
                        <Button Content="Save" Style="{DynamicResource NeonButtonPrimary}"
                                Margin="0,0,8,0" Command="{Binding SaveCommand}" />
                        <Button Content="Toggle Enable" Style="{DynamicResource NeonButton}"
                                Command="{Binding ToggleEnabledCommand}" />
                    </StackPanel>
                    <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource NeonGreen}"
                               FontSize="11" Margin="0,8,0,0" />
                </StackPanel>
            </Border>

            <!-- Test -->
            <Border Grid.Column="1" Style="{DynamicResource NeonCard}" Margin="8,0,0,0">
                <StackPanel>
                    <TextBlock Text="Test Connection" Style="{DynamicResource SectionTitle}" Margin="0,0,0,12" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <Button Content="Validate Config" Style="{DynamicResource NeonButton}"
                                Margin="0,0,8,0" Command="{Binding ValidateCommand}" />
                        <Button Content="Test Send (Dry Run)" Style="{DynamicResource NeonButton}"
                                Command="{Binding TestSendCommand}" />
                    </StackPanel>
                    <TextBlock Text="{Binding TestResult}" Foreground="{DynamicResource NeonCyan}"
                               FontSize="12" TextWrapping="Wrap" />
                </StackPanel>
            </Border>
        </Grid>

        <!-- Setup Guide -->
        <Border Grid.Row="2" Style="{DynamicResource NeonCard}">
            <StackPanel>
                <TextBlock Text="Setup Guide" Style="{DynamicResource SectionTitle}" Margin="0,0,0,8" />
                <TextBlock Foreground="{DynamicResource TextSecondary}" FontSize="13" TextWrapping="Wrap" LineHeight="22">
                    <Run Text="1." FontWeight="Bold" Foreground="{DynamicResource NeonRed}" />
                    <Run Text=" Open NinjaTrader 8 > Tools > Options > Connection" />
                    <LineBreak />
                    <Run Text="2." FontWeight="Bold" Foreground="{DynamicResource NeonRed}" />
                    <Run Text=" Check 'Enable External Connection' (ATI)" />
                    <LineBreak />
                    <Run Text="3." FontWeight="Bold" Foreground="{DynamicResource NeonRed}" />
                    <Run Text=" Default port: 36973 — change above if needed" />
                    <LineBreak />
                    <Run Text="4." FontWeight="Bold" Foreground="{DynamicResource NeonRed}" />
                    <Run Text=" Create routing rules in Rules tab with Destination ID: 3" />
                    <LineBreak />
                    <Run Text="Note:" FontWeight="Bold" Foreground="{DynamicResource NeonAmber}" />
                    <Run Text=" ATI does not support Modify — use Close + re-entry instead" />
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
