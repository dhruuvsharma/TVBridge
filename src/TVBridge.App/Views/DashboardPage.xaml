<UserControl x:Class="TVBridge.App.Views.DashboardPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:TVBridge.App.ViewModels" 
             xmlns:converters="clr-namespace:TVBridge.App.Converters"
             Loaded="OnLoaded">
    <UserControl.Resources>
        <converters:NullToCollapsedConverter x:Key="NullToCollapsed" />
    </UserControl.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Dashboard" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,8" />
            <TextBlock Text="System overview and recent activity" Foreground="#888888" FontSize="14" />
            <Border Background="#FFF3CD" CornerRadius="4" Padding="10,6" Margin="0,8,0,0"
                    Visibility="{Binding UpdateMessage, Converter={StaticResource NullToCollapsed}}">
                <TextBlock Text="{Binding UpdateMessage}" Foreground="#856404" FontSize="12" />
            </Border>
        </StackPanel>

        <!-- Status Cards -->
        <UniformGrid Grid.Row="1" Columns="3" Margin="0,0,0,20">
            <Border Background="#F5F5FF" CornerRadius="8" Padding="16" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="Tunnel" Foreground="#888888" FontSize="12" />
                    <TextBlock Text="{Binding TunnelStatus}" FontSize="18" FontWeight="SemiBold" Foreground="#2D2D44" />
                    <TextBlock Text="{Binding TunnelUrl}" Foreground="#4488CC" FontSize="11"
                               TextTrimming="CharacterEllipsis" ToolTip="{Binding TunnelUrl}" />
                </StackPanel>
            </Border>
            <Border Background="#F5FFF5" CornerRadius="8" Padding="16" Margin="4,0,4,0">
                <StackPanel>
                    <TextBlock Text="MT5 Sidecar" Foreground="#888888" FontSize="12" />
                    <TextBlock Text="{Binding Mt5Status}" FontSize="18" FontWeight="SemiBold" Foreground="#2D2D44" />
                </StackPanel>
            </Border>
            <Border Background="#FFF5F5" CornerRadius="8" Padding="16" Margin="8,0,0,0">
                <StackPanel>
                    <TextBlock Text="Recent Signals" Foreground="#888888" FontSize="12" />
                    <TextBlock Text="{Binding SignalCount}" FontSize="18" FontWeight="SemiBold" Foreground="#2D2D44" />
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Recent Signals List -->
        <GroupBox Grid.Row="2" Header="Recent Signals" Padding="8">
            <DataGrid ItemsSource="{Binding RecentSignals}"
                      AutoGenerateColumns="False" IsReadOnly="True"
                      CanUserSortColumns="True" HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal" BorderThickness="0"
                      RowBackground="White" AlternatingRowBackground="#FAFAFA">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Time" Binding="{Binding ReceivedAt, StringFormat=HH:mm:ss}" Width="80" />
                    <DataGridTextColumn Header="Symbol" Binding="{Binding Symbol}" Width="90" />
                    <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="70" />
                    <DataGridTextColumn Header="Type" Binding="{Binding OrderType}" Width="70" />
                    <DataGridTextColumn Header="Lot" Binding="{Binding LotSize, StringFormat=F2}" Width="60" />
                    <DataGridTextColumn Header="Strategy" Binding="{Binding StrategyId}" Width="*" />
                    <DataGridCheckBoxColumn Header="Done" Binding="{Binding Processed}" Width="50" />
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</UserControl>
